<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:inputLayout="clr-namespace:Syncfusion.XForms.TextInputLayout;assembly=Syncfusion.Core.XForms" xmlns:dropdown="clr-namespace:Syncfusion.XForms.ComboBox;assembly=Syncfusion.SfComboBox.XForms" xmlns:buttons="clr-namespace:Syncfusion.XForms.Buttons;assembly=Syncfusion.Buttons.XForms"
             x:Class="Task.Pages.AddScriptPage" Title="Add New Script">
    <ContentPage.Content>
         <StackLayout Padding="10" Spacing="10">
            <inputLayout:SfTextInputLayout
                Hint="Name" 
                ContainerType="Outlined">   
                <Entry Text="{Binding Name}" />
            </inputLayout:SfTextInputLayout>
            <inputLayout:SfTextInputLayout
                Hint="Code" 
                ContainerType="Outlined">   
                <Entry Text="{Binding Code}" />
            </inputLayout:SfTextInputLayout>

            <dropdown:SfComboBox
                DataSource="{Binding Types}"
               SelectedItem="{Binding SelectedType}"  
                Watermark="Select Type" />

            <buttons:SfButton Text="Save Script" Command="{Binding SaveScriptCommand}"/>
            <buttons:SfButton Text="Cancel" Command="{Binding CancelCommand}"/>


        </StackLayout>
    </ContentPage.Content>
</ContentPage>